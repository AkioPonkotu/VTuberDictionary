"""Command-line entry point for the VTuber dictionary pipeline."""


def main() -> None:
    """Provide a stable entry point while pipeline stages are added incrementally."""
    print("VTuber Dictionary environment is ready.")
